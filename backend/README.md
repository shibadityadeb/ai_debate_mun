# Diplomatrix AI

A modular FastAPI backend skeleton for a UN-style multi-agent debate simulator.

## Run

1. python -m venv .venv
2. source .venv/bin/activate
3. pip install -r requirements.txt
4. uvicorn backend.app.main:app --reload
